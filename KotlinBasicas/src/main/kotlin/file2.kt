

fun main(){
     //when
      /*  val ani= ')'

        println("enter the character: $ani")
          when
       {
           ( ani in 'a'..'z' || ani in 'A'..'Z')->println (" alphabet")
           else->println ("not alphabet")
       }*/
        //when
      /* val month = "jan"
       val type = when(month)
       {
           "jan"  ->"hi"
           "feb"  ->"hii"
           "mar" ->"hiii"
           "apr"->"hiiii"
           else->"byeee"
       }
    println(type)*/
    //print natural num and sum
    /* var num = 1..10
    var sum =0
    for (i in num)
        sum=sum+i
    println(sum)*/
    //factorial using while loop
  /*  val num = Integer.valueOf(readLine())
    var fact =1
    var i = 1
    while(i <= num) {
        fact = fact * i
        i++
    }
    println("factorial of num : $fact")*/
    //factorial using for loop
    /*var num = Integer.valueOf(readLine())
    var fact=1
    for(i in 1..num) {
        fact = fact * i
    }
    println("factorial of num:$fact")*/
    //display character A to Z using loop
   /* val name  = 'a'..'z'
    for (i in name){
        println(i)
    }*/
    //creating calculator using when
   /* var a = Integer.valueOf(readLine())
    var b =Integer.valueOf(readLine())
    println("enter a : $a")
    println("enter b : $b")

    var hii = "mul"
    var new = when(hii){
        "add"->a+b
        "sub"->a-b
        "mul"->a*b
        "div"->a/b
        "module"->a%b
        else-> ("maybe iam in love with you" )

    }
    println(new)*/
    //reverse a number
  /*  var num = 16 downTo 1
    for(i in num)
        println(i)*/
    //reverse a string
/*    var input="sujatha163"
    var output=" "
    val lenght = input.length
    for(i in lenght-1 downTo 0  ) {
        var currentvalue = input[i]
        output += currentvalue
    }
    println(output)
    */
 /* println("enter  : ")
  var a = readLine()
    println("enter:$a")
  println("enter  : ")
    var b = readLine()
    println("enter:$b")*/
    /*var a = Integer.valueOf(readLine())
    var b = Integer.valueOf(readLine())
  if(a<=b){
      println("$a is same ")
  }else{
      println("$b is notsame")
  }*/
    //string convert int
    println("enter a string: ")
    var string_1 = readLine()
    println("enter : $string_1")
    //str convert int
    var suja = String.








}